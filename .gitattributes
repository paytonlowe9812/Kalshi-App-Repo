# Auto detect text files and perform LF normalization
* text=auto

# Omit from `git archive` (GitHub Release ZIPs/tarballs); generated locally via install.* or generate_launchers.py.
PredictionMarketApp/launch.bat export-ignore
PredictionMarketApp/launch.sh export-ignore
